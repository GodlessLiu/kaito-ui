// const namespace = "ka";

// export function useNameSpace(component: string) {
//   const prefix = `${namespace}_${component}`;
//   return function (fallow: string = "") {
//     return `${prefix}-${fallow}`;
//   };
// }
